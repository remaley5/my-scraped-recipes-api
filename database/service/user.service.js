const userRepository = require("../repository/user.repository");
const logger = require("../logger/api.logger");

class UserService {
    // include password hashing and error handling
}

module.exports = new UserService();
